# ---------------------------------------------------------------------------
# Sledje frontend environment
# Copy to .env and fill in.
# ---------------------------------------------------------------------------

# WARNING: this key is NOT currently read by any code.
# src/api.js hardcodes its baseURL to the production Render host, so local
# development requires editing that file by hand. See docs/08-frontend.md.
REACT_APP_API_URL=http://localhost:5000
